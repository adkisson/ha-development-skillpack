# Chatter Control (Patterns)
- Debounce per room/zone; coalesce & act once.
- Rate‑limit; prefer event‑driven to time‑pattern triggers.
- Group first (Zigbee/Z‑Wave groups or HA areas).
- **Logging**: only during debugging; production logs reserved for significant issues.
